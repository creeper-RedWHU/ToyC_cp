// 测试一元 + 运算符
// +x 等价于 x，不改变符号
// 预期：+5 + +(+3) + +(-2) = 5 + 3 + (-2) = 6
int main() {
    int a = +5;
    int b = +(+3);
    int c = +(-2);
    int d = +(a + b);    // +(5+3) = 8
    int e = + +c;        // + +(-2) = -2
    return a + b + c + d + e;   // 5 + 3 + (-2) + 8 + (-2) = 12
}
